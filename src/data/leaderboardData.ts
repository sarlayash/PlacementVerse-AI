import { LeaderboardEntry } from '../types';

// Real leaderboard pool is dynamically populated and ranked from actual registered students
export const INITIAL_LEADERBOARD_POOL: LeaderboardEntry[] = [];

